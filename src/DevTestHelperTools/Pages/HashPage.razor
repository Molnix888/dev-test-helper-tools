@page "/hash"

@using Microsoft.AspNetCore.Components.Forms
@using Microsoft.AspNetCore.Components.Web

<EditForm Model="@Hash" OnValidSubmit="ComputeHash">
    <DataAnnotationsValidator />

    <div class="select-container">
        <InputSelect id="hashType" required @bind-Value="Hash.Algorithm" @onclick="SelectMenu.ToggleArrow" @onfocusout="SelectMenu.ToggleArrowClosed" @onkeydown="SelectMenu.ToggleArrowOnKeys" @onkeyup="SelectMenu.ToggleArrowOnKeys">
            <option value="">Select algorythm</option>

            @foreach (var hashType in _hashTypes)
            {
                <option value="@hashType">@hashType</option>
            }
        </InputSelect>

        <i class="fas fa-chevron-@SelectMenu.SelectMenuIconClass"></i>
    </div>

    <div class="textarea-container">
        <InputTextArea id="input" placeholder="Enter your text here..." @bind-Value="Hash.InputString" />
    </div>

    <div>
        <button type="submit" id="generate">GENERATE</button>
    </div>

    <div class="text-data">
        <p>
            <label for="source">Entered value:</label>
            <div id="source" class="text-block">@Source</div>
        </p>

        <p>
            <label for="result">Hash:</label>
            <div id="result" class="text-block">@Result</div>
        </p>
    </div>
</EditForm>
